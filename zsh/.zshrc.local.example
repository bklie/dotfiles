# ================================================
# ローカル設定（マシン固有）
# ================================================
# このファイルを ~/.zshrc.local にコピーして使用
# gitignore対象のため、マシン固有の設定を記述
#
# 使い方:
#   cp ~/.zshrc.local.example ~/.zshrc.local
#   nvim ~/.zshrc.local
# ================================================

# ================================================
# PATH設定（マシン固有）
# ================================================

# 例: PHP (Homebrew)
# export PATH="/opt/homebrew/opt/php@8.2/bin:$PATH"
# export PATH="/opt/homebrew/opt/php@8.2/sbin:$PATH"

# 例: Composer
# export PATH="$HOME/.composer/vendor/bin:$PATH"

# 例: Node.js (nodebrew)
# export PATH="$HOME/.nodebrew/current/bin:$PATH"

# 例: Python (pyenv)
# export PYENV_ROOT="$HOME/.pyenv"
# export PATH="$PYENV_ROOT/bin:$PATH"
# eval "$(pyenv init -)"

# ================================================
# エイリアス（マシン固有）
# ================================================

# 例: プロジェクト固有のショートカット
# alias myproject='cd ~/projects/my-project && docker compose up -d'

# 例: 会社固有のコマンド
# alias vpn='open /Applications/CompanyVPN.app'
# alias jira='open https://jira.company.com'

# ================================================
# 環境変数（マシン固有）
# ================================================

# 例: プロキシ設定
# export http_proxy="http://proxy.company.com:8080"
# export https_proxy="http://proxy.company.com:8080"

# 例: APIキー（セキュリティ上、ここには直接書かないこと推奨）
# export OPENAI_API_KEY="your-api-key"

# ================================================
# その他
# ================================================

# 例: conda初期化
# __conda_setup="$('/opt/anaconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
# if [ $? -eq 0 ]; then
#     eval "$__conda_setup"
# fi
# unset __conda_setup

echo "Local config loaded"
