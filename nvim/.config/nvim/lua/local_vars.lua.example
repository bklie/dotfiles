-- ================================================
-- ローカル変数設定（プラグイン読み込み前）
-- ================================================
-- このファイルを local_vars.lua にコピーして使用してください
-- local_vars.lua は .gitignore に登録されているため、Git管理対象外です
--
-- 使い方:
--   cp lua/local_vars.lua.example lua/local_vars.lua
--
-- Note: このファイルはプラグイン読み込み前に実行されるため、
--       プラグインの設定に影響する vim.g.* 変数をここで設定します
-- ================================================

-- ================================================
-- Sidekick AI設定
-- ================================================
-- デフォルトのAIエージェントを変更（デフォルト: claude）
-- 利用可能: aider, amazon_q, claude, codex, copilot, crush, cursor, gemini, grok, opencode, qwen
-- vim.g.sidekick_default_cli = "copilot"
